{% assign directoryPrefix = "" %}
{% for i in (1..level) %}
{% assign directoryPrefix = directoryPrefix | append: "../" %}
{% endfor %}

<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>{{ title }}</title>
<link rel="stylesheet" href="{{ directoryPrefix }}static/style.css">
</head>
<body>

<header><a href="{{ directoryPrefix }}blog.html">My Blog</a></header>

<main>
{{ content | raw }}
</main>

</body>
</html>
